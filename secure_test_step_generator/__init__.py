"""Secure Test Step Generator.

Standalone pipeline: Document -> OCR -> Sanitization -> AI Step Generation
-> Validation -> Excel Export.

This package intentionally has no import dependency on the rest of the
qa-ai-copilot repository, so it can be cloned/copied and run on its own.
"""

__version__ = "1.0.0"
